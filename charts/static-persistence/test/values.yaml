nameOverride: mariadatenbnk

pvc:
  enabled: true
  annotations: {}
  requests:
    storage: 2Gi
  accessModes:
    - ReadWriteMany
  storageClassName: ceph-filesystem
pv:
  enabled: true
container: 
  enabled: true